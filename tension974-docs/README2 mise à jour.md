j'ai ajouter une clé API firecrawl dans secrets.env
ça te permettra de tester jusqu'au bout le code.
aussi ajoute un systeme de suivi des credits firecrawl a la suite de chaque requete 
et dans le dashboard
fais le minimum de requete firecrawl car le credit est limité
Une fois que le scrapping est fonctionnel via firecrawl fait des tests en direct sans firecrawl pour récuprer le contenu afin deconomise le credit