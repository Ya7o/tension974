FIRECRAWL_API_KEY=
# Backend de production : JSONL versionné par git.
DATA_DIR=data
SEARCHES_CONFIG_PATH=config/searches.yaml
LOG_LEVEL=INFO
# Uniquement pour le backend SQLite optionnel (--storage sqlite, dev local).
DATABASE_PATH=data/tension974.db
